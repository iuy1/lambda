#import "/src/lib.typ": *

#show raw.where(block: true): set block(
  fill: luma(244),
  inset: 6pt,
  radius: 3pt,
  width: 100%,
)

= diagram

```typ
#display(const.omega)
```
#display(const.omega)

```typ
#diagram(const.omega)
```
#diagram(const.omega)

```typ
#display(const.fact)
```
#display(const.fact)

```typ
#diagram(const.fact)
```
#diagram(const.fact)
